# inca-rag-scope Production Database Environment Variables
# Copy this file to .env and fill in the values
#
# IMPORTANT: Never commit .env to git (already in .gitignore)

# PostgreSQL Database Configuration
POSTGRES_DB=inca_rag_scope
POSTGRES_USER=inca_admin
POSTGRES_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# PostgreSQL Port (host port mapping)
# Default: 5432 (standard PostgreSQL port)
# Change if you have another PostgreSQL instance on the same host
POSTGRES_PORT=5432
