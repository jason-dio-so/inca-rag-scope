# ========================================
# DB SSOT LOCK — Single Source of Truth
# ========================================
#
# ALL database connections in this project MUST use these variables ONLY.
# NO exceptions. NO temporary workarounds.
#
# Enforcement: DB ID CHECK at every entry point (apps/api, pipeline, tools)
# Validation: tools/gate/check_db_ssot_lock.sh

# ✅ ALLOWED (ONLY)
SSOT_DB_URL=postgresql://postgres:postgres@localhost:5433/inca_ssot
SSOT_DB_HOST=localhost
SSOT_DB_PORT=5433
SSOT_DB_NAME=inca_ssot
SSOT_DB_USER=postgres
SSOT_DB_PASSWORD=postgres

# Expected DB ID CHECK values
SSOT_DB_EXPECTED_NAME=inca_ssot
SSOT_DB_EXPECTED_PORT=5433

# ❌ FORBIDDEN (legacy/other)
# DO NOT USE: postgresql://inca_admin:inca_secure_prod_2025_db_key@localhost:5432/inca_rag_scope
# DO NOT USE: Any connection to port 5432
# DO NOT USE: Any connection to database inca_rag_scope

# Gate enforcement: tools/gate/check_db_ssot_lock.sh
# - Blocks any code containing: inca_rag_scope, :5432/, localhost:5432, inca_admin:inca_secure_prod_2025_db_key
# - Whitelist exceptions: docs/policy/*.md (documentation of forbidden patterns)
